// Mantido como ponto de import estável; a definição real (parametrizada,
// com default acompanhando a porta atual da stack) vive em lib/env.ts —
// ver S-04 da auditoria.
export { BACKEND_INTERNAL_URL } from "@/lib/env";
