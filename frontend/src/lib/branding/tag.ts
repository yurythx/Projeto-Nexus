/** Tag do cache de dados do Next para GET /branding (layout, metadados e
 * páginas públicas). Salvar o branding a expira na hora — ver
 * components/settings/brandingActions.ts. */
export const BRANDING_TAG = "branding";
